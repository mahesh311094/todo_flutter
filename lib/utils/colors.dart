import 'package:flutter/material.dart';

// this is color file which contains all color used in app
const primaryColor = Color(0xFF1A5277);
const secondaryColor = Color(0xFFBFEDFF);
const textColor = Color(0xFF212121);
const greyColor = Color(0xC56F6F6F);
const darkGreyColor = Color(0xFF6F6F6F);
const whiteColor = Colors.white;
const blackColor = Colors.black;
const errorColor = Colors.red;
const linkColor = Colors.blueAccent;
const transparentColor = Colors.transparent;
